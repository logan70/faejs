/**
 * Parameter type.
 */
export enum ParamType {
  query = 'query',
  body = 'body',
  param = 'param',
  headers = 'headers',
  // file = 'file',
  // files = 'files',
  // session = 'session',
  // sessionParam = 'sessionParam',
  // state = 'state',
  cookies = 'cookies',
  request = 'request',
  response = 'response',
  ip = 'ip',
  context = 'context',
  custom = 'custom',
}
